# State (reserved)

This directory is reserved for behavior state (v1.1+). A planned addition is a small `data.json` ledger to implement per-directory sleep/debounce for WM chain injection. In v1.0 this directory is empty and has no runtime effect.
